import NavBar from "./NavBar";

// Header Component. Only contains NavBar but left as component incase of later changes.
function Header() {
    return (
        <NavBar />
    )
}

export default Header;